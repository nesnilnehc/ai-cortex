# Changelog

## [Unreleased]

### Added

- `scripts/check-asset-versions.py` blocks a merge when an asset the range edits kept its version, and runs in CI.

  The obligation was written down in the releasing guide and enforced by nothing. It caught `orchestrate-code-review` gaining a dispatch step and keeping 1.2.0 through a fully green run — found by hand during the 0.3.0 release, which is one release later than a check would have found it. Run against that range it reports the defect and exits 1. What it cannot decide is whether the size of a bump is right; that stays with a reader.

  Its CI job checks out with `fetch-depth: 0`, because the check diffs against the most recent product tag and a shallow checkout fetches none. With no tag reachable it prints `NOTHING CHECKED` and fails rather than passing on an empty range.

### Fixed

- `validate-rules.py` no longer lets a Rule that means to be modeled drop silently out of validation.

  A leading blank line before the frontmatter, or `RULE_MODEL_V1` spelt with one letter's wrong case, took a Rule out of the modeled set. The run then validated nine documents instead of ten, printed the lower number and exited 0. Both were verified by breaking a real file. A file that declares some other `model` value, or that carries `### ABC-001 —` item headings without parsing as modeled, is now an error naming the file and what to check.

- The two scripts that read JSON fixtures now name the file when one is malformed.

  `test-rule-scenarios.py` and `test-research-artifacts.py` called `json.loads` bare, so a broken fixture produced a `JSONDecodeError` carrying a line and column and no path, or a `KeyError` naming a key and no file — unusable when three directories of fixtures could be the one at fault. `validate-research-artifacts.py` and `verify-translation.py` already did this correctly; the two stragglers now match them.

  Found by running the new `error-surfacing-quality` items against this repository's own `scripts/`, which is the first time they were pointed at anything.

## [0.3.0] — 2026-09-18

### Added

- A tenth modeled Rule set governs where a defect is decided and whether the failure that results can be acted on: `error-surfacing-quality`, prefix ERR, six items, with `review-error-surfacing` to execute it.

  The four obligations are deciding untrusted input at the boundary it enters, stopping once an invariant is known broken, detecting at the earliest declared layer, and giving a person the next action or a program a stable identifier. The Skill joins `orchestrate-code-review` as a seventh cognitive step.

- `orchestrate-code-review` 1.2.0 → 1.3.0 for the dispatch step it gained.

  A new step is a MINOR bump under CONTRIBUTING's versioning rules. Caught by the release gate rather than by a check: the file was edited in this range and its version was not, which is exactly the thing a consuming project would read wrong.

- ERR-006 turns this repository's own experience into an obligation: a check whose findings are mostly legitimate use must be narrowed, retargeted or withdrawn rather than left for readers to filter.

  A report that is mostly legitimate teaches its readers to skim, which costs more than the check was ever worth.

- The Rule states its boundary against its three neighbours rather than leaving a reviewer to find it.

  Retry and partial-failure behaviour is reliability's, the telemetry event is observability's, and disclosure is security's. Observability is the confusable one, so the Rule and the Skill each spend a sentence on it — this reads the message handed back to whoever asked, that reads the event written for whoever operates.

- Every ERR reference carries a verified locator rather than a remembered one.

  RFC 9413 for why tolerant acceptance decays a protocol, Shore's *Fail Fast* in IEEE Software 21(5) for failing immediately and visibly, Nielsen's ninth heuristic for what a message owes a person, King's *Parse, don't validate* for deciding at the boundary, and Boehm and Basili's 2001 list for the cost argument — recorded as a range, 100:1 on large projects and 5:1 on small non-critical ones.

- The unattributed "100x" cost chart must not ground a finding, and is recorded as such.

  It has no traceable data behind it, and the institution named as its source ran an internal training programme. Bossavit's *Leprechauns* is the counter-evidence; the measured range above is what a finding cites instead.

- The source map gains a section for failure surfacing, mapping each of the six sources to the ERR items it informs.

  In `docs/references/software-engineering-classics.md`. Most of these are not books: the current normative statement is an RFC and the human-facing criterion comes from empirical usability work, both of which the evidence hierarchy places ahead of a classic on this topic.

- The source map also names the three ideas here that have no citable source, rather than dressing them as authority.

  "Make illegal states unrepresentable" is community vocabulary. The four elements of a good diagnostic are this repository's reading of Elm and Rust compiler output, not a finding either project published. ERR-006's threshold reasoning rests on measurements taken here, which the hierarchy already places at its fourth tier.

- `docs/guides/releasing.md` writes down how this repository is released, linked from the README and from a new Releasing section in `CONTRIBUTING.md`.

  It is a usage map for `prepare-release` and `publish-release`, not a second definition of them: which Skill owns which step, what counts as worth releasing, why the asset version domains do not follow the product version, and the three details that had each been got wrong once — the manifest status, the tag sitting one commit past the recorded commit, and where the release notes live.

- The release criterion counts what a consumer consumes, which is five things and not four.

  `cortex update` hard-resets the whole clone, so every tracked file arrives; arriving is not the test. `skills/` and `rules/` are installed into the IDE directories, `specs/` and `protocols/` are read from the clone, and `bin/cortex` is itself linked at `~/.local/bin/cortex` — so editing the installer changes the command someone runs, while falling outside all four directories the first draft named.

- `review-error-surfacing` carries a worked pass and a worked failure for each of its five judgment items, and ERR-006 names the tool class behind its count.

  Constraint 8 of `workflow-rule-governance` makes that the activation evidence a judgment item owes, and the approved rule-governance design says the same in its test strategy. The Skill had shipped active with the pair for ERR-001 only, one failure for ERR-002, nothing for ERR-003, and an applicability note standing in for ERR-004 and ERR-005. Thirteen examples now, from five, and the section names the constraint it discharges so a reader can audit the claim.

### Fixed

- `review-error-surfacing` states its evidence order, which it had omitted: prefer a check the repository already runs, otherwise read the scope, and either way say what the evidence does not cover.

  `review-architecture` has carried that shape since the Rule sets were externalised; the new Skill listed what to look at without saying that a tool is used when present and not required when absent. The third clause is the one no scanner supplies — a reading of a diff decides what that change introduces and settles nothing about the rest of the codebase, and a clean tool run settles nothing about paths the tool cannot resolve.

- `standards-coding` 1.0.2 → 1.1.0 points at two obligations instead of restating them.

  Its error-handling constraint carried "fail fast" and "messages carry context and a suggested resolution", which `error-surfacing-quality` now defines precisely and waivably; its simplicity constraint carried "keep no commented-out dead code", which ARC-010 defines. Both duplications arrived with the new items, written without checking what already said the same thing. What is unique to this document stays: one consistent mechanism, and resources released on the error path. The pointers earn their cross-links because this Rule is user-scoped and installed everywhere while both targets are project-scoped, so a reader who loads only this one still learns where the obligation lives.

- `standards-coding` 1.1.0 → 1.1.1 stops repeating itself.

  Its simplicity and complexity constraints both said duplicated code must be extracted and stale comments deleted. A comment obligation now lives with the other comment obligations, DRY lives in simplicity, and the complexity constraint keeps only what is actually a threshold — 50 lines and three levels of nesting. This overlap predates the two cross-document ones above, which arrived with the new Rule set.

## [0.2.1] — 2026-09-18

### Added

- Five rules take a patch bump for the edits in this release: `standards-coding` 1.0.2, `standards-import` and `standards-shell` 1.0.1, `writing-chinese-technical` 1.3.1, `workflow-documentation` 1.1.1. A rule's version is what a consuming project reads to decide whether to re-read it, so an edited rule whose version says otherwise is the one lie worth avoiding.
- Four rules gain the `artifact_type` and `status` their 25 peers carry. `standards-coding`, `standards-import`, `standards-shell` and `writing-chinese-technical` predate the convention and were the only rules without them; every rule now declares all five fields the unmodeled ones use. Found by sweeping all 29 rather than reading them: the citation checks came back clean — every backticked repository path resolves, every Rule ID cited anywhere is defined, and the two skill names that look wrong are a renamed skill quoted on purpose in an ADR counter-example.
- `workflow-documentation` records which of its own patterns CI runs and which need a reader, and fixes one that could not be copied. The version-narration pattern added earlier contains a backtick and was written inside a single-backtick span, where a backslash does not escape it — the span closed early and the pattern rendered wrong, so anyone copying it got a broken regex. It is a double-backtick span now, and all 35 patterns in the section compile. The split: the filename, version-narration and process-vocabulary patterns of both corpora run, along with the English conversational residue. The Chinese residue and heading suffixes do not, because they match 0 and 3 times repository-wide and all 3 sit in exempt immutable records — automating them adds a check that fires on nothing. Conversational shorthand does not either: it matches 156 times in live documents, nearly all defined terminology, and the criterion's own "unless defined in this very document" clause is not something a grep can decide.
- `scripts/check-cortex-docs.py` compares the prose about `bin/cortex` with the script. Three audits in one day found the same shape of defect — the assets are guarded and correct, the prose describing the installer had drifted behind it — and two parts of that are decidable: a document must not name a subcommand or flag the script lacks, and a subcommand the script offers must be documented somewhere. Whether "pulls the latest commit" fairly describes a hard reset still needs a reader; `cortex sync` sending someone to a command that does not exist does not. It reads only fenced blocks and inline spans, because the first draft read prose too and would have reported "bin/cortex run from inside a clone" as a subcommand — that sentence is now in the clean fixture, so the mistake fails there rather than here.
- `scripts/check-asset-counts.py` guards the README's counts, which were written by hand in a sentence and again as a column in the capability table, with nothing comparing either against the repository. It reports three kinds of drift: a declared count that no longer matches the directory it describes, a layer the README stopped mentioning, and a table column that no longer sums to the skill total. It does not judge which area a skill belongs to — that is a reading of the skill, not something a path settles — only that the column adds up. A clean tree and a drifted one give the test its denominator, and six mutations join the harness; one survived the first run because every fixture skill held a single document, so counting documents and counting directories agreed, and a real skill carries a README beside its SKILL.md.

### Changed

- The README says what `cortex update` does to a clone. It described it as pulling the latest commit; it fetches and then hard-resets onto `origin`, which discards local commits on the tracked branch and, with `--force`, uncommitted work as well. The dirty-tree refusal that makes the default safe was not mentioned either. The Quick start also now says `cortex install` puts the command at `~/.local/bin/cortex` and warns when that is not on `PATH` — every command after the first was written as a bare `cortex` with nothing explaining where it came from.
- `AGENTS.md` describes how rules actually reach an agent. It said `specs/`, `protocols/` and `rules/` are all read straight from the install path with no install step, which holds for the first two and not for rules: `bin/cortex` symlinks the 20 rules scoped `user` or `both` into each IDE's own rules directory, where they load as long-lived context, and leaves the 9 scoped `project` in the clone for on-demand loading. An agent following the old sentence would go looking in the install path for rules already loaded around it. The path resolution also gains its third tier, running from a clone.
- `CLAUDE.md` stops telling an agent there is nothing to run. It said the repository was not a code project and that `package.json` had an empty `scripts` — there is no `package.json` at all, it went with the agent.yaml and manifest mechanisms, and there are now fourteen Python scripts of which CI runs thirteen. The instruction not to reach for npm stays, with a reason that is true; the briefing points at the checks and names `scripts/` and `tests/fixtures/` as working areas beside the markdown. The registry trap also says the skills index is generated and that the other three are maintained by hand.
- `docs/designs/README.md` tells the next author to write for someone who took part in no conversation. A snapshot is not revised once dated, so a sentence leaning on a discussion the reader cannot reach stays unhelpful permanently — the research technical design argues against a layout "from the earlier discussion" and nothing records which one. The existing snapshot stays as written; rewriting its prose would falsify what it recorded. CI cannot catch this either, because a dated design in that directory is a labelled temporary document, which is what it is entitled to be.
- The README states the protocol layer's count alongside the other three. One protocol is not a gap in a layer that needs a single document, and leaving the number out read as an omission.

## [0.2.0] — 2026-09-18

### Added

- 语言方案反转:仓库主语言从中文切换为英文。`docs/LANGUAGE_SCHEME.md` 重写为英文并反转原则——英文为默认,中文仅保留于**不可变记录**(既有 ADR、已发布 CHANGELOG 条目、设计快照)。这三类按 `rules/adr-management.md`「写入后不改」保留原文,改写它们等于伪造时点记录。新增第 6 节「翻译质量门槛」:明确禁止机器翻译,要求「重写而非翻译」——本仓库已为机翻付过一次代价(164 处重复直译标题、27 处误译),反向迁移不能重演。第 7 节列出 E0–E6 分阶段迁移表,每阶段独立可发布;`skills/INDEX.md` 与全部 frontmatter 全程保持英文,因此迁移期间发现与技能匹配不受影响。
- E1 阶段完成:`README.md` / `AGENTS.md` / `CONTRIBUTING.md` / `SECURITY.md` / `CODE_OF_CONDUCT.md` 改写为英文。CONTRIBUTING 新增一条硬要求——外部派生技能的**许可证兼容性**:本仓库为 MIT,派生自 copyleft 或非商业上游(CC BY-NC-SA、AGPL 等)的技能无法以 MIT 再分发,不予接受,须在动手前而非事后核对上游许可证。README 标题变更导致 15 个技能 README 的锚点 `#-安装与使用` 失效,已同步更新为 `#-install-and-use`。
- Engineering review criteria move out of the Skills that executed them and into versioned Rule sets. They had lived inside the cognitive review Skills, so they could not be applied before coding, configured per project, or cited afterwards. `specs/rule-modeling.md` defines `RULE_MODEL_V1` — stable item identifiers that are never reused, three applicability levels, parameter provenance and the waiver object — and nine Rule sets now carry 102 items across architecture, security, reliability, performance, observability, testing, implementation alignment, task quality and technical design quality. Of those, 53 are baseline and apply wherever the set's scope is present, 42 are gated on a profile and 7 on a declared project parameter. Severity is graded by what happens if a finding is left unfixed, so 63 block the gate rather than all 102: absent evidence such as a missing test or measurement raises future risk rather than causing a present failure, and is minor. `rules/workflow-rule-governance.md` constrains identifier stability, compatibility and the activation evidence each enforcement strength owes — and says when a set should adopt the model at all, which is only when its items must be waived, counted in coverage or cited by another artifact, so requirement and functional design quality deliberately stay checklists. Six review Skills are new (functional design, technical design, tasks, implementation alignment, observability, reliability) and four cognitive reviewers now execute the canonical Rules instead of their own embedded lists. `scripts/validate-rules.py` validates every modeled document and item, and `scripts/test-rule-scenarios.py` decides the 15 items that are both automated and blocking over nine representative shapes drawn from three populations — a library, a layered application and a distributed service. Both run in CI. The four rewritten reviewers went to a major version — `review-architecture`, `review-performance`, `review-security` and `review-testing`, with `review-requirements` alongside them — although their `code-scope → findings-list` contract did not change: what changed was the authority their criteria come from and what completeness now means, which is breaking for a caller relying on the old embedded checklists even though the artifact types still match. Recorded in `docs/adr/0012-adopt-profiled-engineering-rules.md`.
- Five research entries, an internal assessment, and the two contracts they pass between them. `deep-research`, `policy-research`, `market-research`, `competitive-research` and `product-opportunity-analysis` are user-invocable; `assess-product-opportunity` carries the recommendation logic and is internal by repository metadata, while the package entry selects the relevant lanes and aggregates their output. `specs/research-evidence.md` defines a portable Research Report whose decision-relevant statements trace to a source or to an explicit gap, and `specs/opportunity-package.md` the decision layer built on it. `docs/adr/0013-research-skill-entry-names.md` records the narrow naming exception: four of the five names are noun-led and the package entry lacks the usual `orchestrate-` prefix, which is kept because renaming would break commands already promised to users, and it does not loosen the verb-noun rule for anything else. `scripts/test-research-artifacts.py` validates 10 positive fixtures and 26 negative cases in CI, including a conflicting report, a stale source and an inaccessible one. Live routing through an authenticated host is not yet verified; `docs/references/research-skills-smoke-test.md` separates what was checked locally from what still waits.
- Two criteria for code that has outlived its use, which the Rule sets had no item for. `architecture-quality` 1.0.1 → 1.1.0 adds ARC-010 and ARC-011, bringing the nine sets to 104 items. ARC-010 requires code no consumer reaches to be removed rather than commented out, disabled behind a permanent flag or kept in case it is wanted again, and fixes what counts as knowing it is unreached: unused-symbol analysis over the built artifact, plus a runtime signal over one full business cycle for anything the analyzer cannot resolve — reflection, a class named in configuration, dynamic dispatch, a consumer in another repository. Its pass condition says outright that a clean analyzer run is not evidence about a symbol the analyzer cannot see, which is the failure that deletes something month-end still calls. ARC-011 requires a deprecation marker to record who removes it, by which release or date, and what a caller migrates to; a marker carrying none of the three is a second implementation kept indefinitely rather than a migration in progress. Both are `minor` and `tool-assisted`, graded by the set's own rule that debt raising future cost does not block a gate the way a present failure does. `review-architecture` picks them up with no change to its execution, since it runs whichever items the active Rule version carries; 2.0.1 → 2.1.0 adds a worked example for each, because both turn on a distinction a reviewer gets wrong by default — an analyzer reporting nothing is not a pass for what it cannot resolve. Neither owes a fixture test — constraint 8 of `rules/workflow-rule-governance.md` asks that of automated blocking items — but the half of each that is mechanical, deciding the obligation once the evidence is in hand, is pinned in `scripts/test-rule-scenarios.py` regardless, over a fourth shape that brings it to 10: a plugin host that loads handlers by name from configuration, where static analysis is weakest. Writing those cases found a hole in ARC-010 as first published: its pass condition let any removal through, so deleting a reflection-reached symbol on the analyzer's word alone would have passed — the exact failure the item was written to prevent. The condition now requires a runtime signal in that direction too. Seven mutations of the decision were seeded and all seven were caught. `review-implementation-alignment` 1.0.0 → 1.1.0 states the boundary rather than taking the items on: delivered code nothing reaches is its own, under ALN-003, but the implementation a change replaced and left behind is ARC-010's, decided by the sibling engineering gate over the same scope. The two read alike and would otherwise be reported twice or by neither. `review-codebase` 1.0.1 → 1.1.0 names both in its tech-debt dimension, which is where a reader first meets them, and routes the decision rather than taking it: neither is settled by reading the code, since one needs a runtime signal and the other needs the fields recorded beside the marker.
- `CONTRIBUTING.md` gains a Checks section. There had been no list: the scripts were named in one sentence inside "Adding a skill", which is the wrong home for a checker that has nothing to do with skills, and a contributor changing a Rule or a script had nothing to go on. The table says which check to run for which kind of change, and `mutation-check.py` carries a paragraph of its own, because "run the thing that checks the checks" earns an explanation rather than a line in a table.
- `scripts/mutation-test.sh` is folded into `scripts/mutation-check.py`, which now covers both shapes of perturbation under one frame: a defect seeded into a checker, and an edit made to an already-translated document that the translation verifier must block. Folding it in is how its state was discovered — eleven of its fifteen cases had stopped matching `rules/task-quality.md`, which became a modeled Rule set while the cases still quoted its old checklist form. It skipped them and reported `4/15`, and it was never in CI, so nobody read the number. The anchors are rewritten against the current document, and two were retargeted after the run showed them landing on a soft invariant: a dropped table row and a dropped heading are reported for judgement, not blocked, so the cases now break frontmatter and block count instead — the verifier was right and the expectation was wrong. Forty-three perturbations, all producing the expected verdict, including two controls that must be allowed through.
- `scripts/mutation-check.py` makes the mutation runs repeatable and puts them in CI. They had been throwaway scripts written beside each test and discarded, so the claim that a suite can fail was true on the day and unverifiable afterwards. Twenty-eight defects are seeded — 7 into the link checker, 14 into the document-hygiene checker, 7 into the Rule scenario evaluator — and each must turn its test red. A mutation whose pattern no longer matches is a failure rather than a skip, because a refactor that moves the code takes the coverage with it while the run stays green. Where the same line appears in two functions, a scope marker picks the one under test; without it the first occurrence absorbed the mutation and a branch went untested, which had already happened once. The harness was itself checked against its three failure modes: a no-op mutation, a stale pattern and an unfindable scope.
- The link checker's fixtures grow from two seeded defects to seven mutations, matching the other three checks. The tree gains an anchor that must still resolve, an absolute URL and a mail address that are not repository paths, and a guide outside `rules/`, `specs/` and `protocols/` linking a Skill directly — the reference-direction rule binds governance assets, not prose, and nothing had been asserting that. All seven are caught: the code-span strip, the fence matched at any indent, the fence closed on its own marker length so a longer one can hold a shorter, the fence skip itself, the absolute-link exclusion, the anchor strip, and the governance gate.
- The reachability and filename checks get the fixture tree they had been running without. `tests/fixtures/doc-graph` carries an orphan, a tombstone unlinked on purpose, a directory README that is an entry point rather than an orphan, a backup beside the file it copies, a link shown inside a fence that must not make its target reachable, and two lowercase names — `review-security.md` and `release-notes.md` — that the uppercase process-record words must not fire on. Seven mutations were seeded and all seven caught, but three only after the shapes were extended: nothing had distinguished a legitimate lowercase name whose token is on the list, and nothing had exercised the fence skip. A fourth mutation survived because the code it targeted reached nothing — resolving a link to a directory into that directory's index, which changes no reachability when every index and README is already an entry point. It is deleted rather than given a fixture, on the same reasoning ARC-010 states.
- The temporary-document patterns are enforced, not just written down. `scripts/check-doc-hygiene.py` gains a third check and the CI step covers it, decided the way the rule reads: a body pattern identifies a document as temporary, and constraint 4 then requires such a document to carry a dated or `.draft` name inside a dedicated directory. The finding is therefore an unlabelled one — the same sentence is compliant in `docs/designs/2026-09-17-…` and a defect in a Spec. An ADR is exempt because narration is its genre, the changelog because it is narration throughout, and a pattern is exempt where it is shown rather than used: under an anti-pattern heading, marked with a cross, inside a fence, or inside quotation marks. That last one carries most of the weight, since `write a rule, not "we decided to follow the convention"` quotes what it forbids. `scripts/test-doc-hygiene.py` pins it over ten shapes, and seven mutations of the decision were seeded and all seven caught — two of them only after the shapes were extended, because a dated name outside a dedicated directory had no fixture and one mutation had been silently rewriting the wrong function.
- Eleven review Skills drop their `## Change record` section, each taking a patch bump. `rules/workflow-documentation.md` treats version narration in a body as a temporary-document marker and sends it to the changelog, and the sections had drifted into exactly that — "Version 2.0.1 removes a stale example pin" and the like. The descriptive bullets beside them restated what the frontmatter and the body already say. One thing they held was recorded nowhere else and is preserved above: why four reviewers took a major version although their artifact types stayed compatible. The rule's own grep list was Chinese-only and caught none of them.
- `workflow-documentation` 1.0.0 → 1.1.0 enumerates the English grep set the rule had been promising, for both body patterns and conversational residue. Every candidate was measured against the repository before it went in, and two were dropped on the numbers: `\bTBD\b` fired 19 times, nearly all of them Skills telling an agent to write `TBD` where a value cannot be inferred, and `\bto be built\b` fired on a metric whose data source is genuinely pending — a pattern whose hits are mostly legitimate only teaches a reader to skim past the report. The finished set matches two documents across 244. Neither is a violation: the rule's constraint 4 says a body pattern identifies a document as temporary and such a document must then be labelled, so a dated design snapshot citing "the earlier discussion" is compliant, and the other hit quotes the form inside quotation marks. The exception for a pattern shown as a counter-example widens from this rule alone to any Rule or Spec quoting one in a labelled anti-pattern list, which is what four of the six raw hits were.
- Enforcement for the documentation criteria that had none. `rules/doc-health-criteria.md` §2 and `rules/repo-structure-hygiene.md` §4 defined what an orphaned document and a stale filename are, but nothing ran them: `plan-next` routes both to "the runtime / linter / CI", and there was no such check. `scripts/check-doc-hygiene.py` now decides the part a path and a link graph can prove — reachability from the README or a directory's index, backup suffixes, and process-record words matched only in uppercase so the 26 `review-*` Skills are not swept up. A document marked `status: deprecated` is exempt, because a tombstone such as `docs/guides/protocols-registry.md` is unlinked on purpose, to catch a bookmark arriving from outside. Eight real orphans were found and cleared; four design snapshots and a smoke record now hang off a new `docs/designs/README.md`. Forward-tested against five shapes before wiring, which caught it missing `usage.md.bak`, since a backup file is not a `.md` file.

### Fixed

- Eight dead links under `docs/adr/`, and the CI allowlist that excused them. The link job skipped every broken link in that directory, on the stated grounds that older records deliberately reference assets since deleted. Six of the eight were not that: written as `../../../specs/skill.md` from `docs/adr/`, they climb past the repository root and never resolved, one of them beside a correctly written link in the same table. Each is downgraded to its label text under the maintenance `rules/adr-management.md` §4 now permits — no word of any record changed, and no link repointed at a renamed successor. The allowlist is gone, so a broken link anywhere fails the build.
- The numbered procedures in `docs/guides/protocols-usage.md`, whose rendered numbering did not survive. Each step's code block sat at column zero, and a fence at column zero ends the list, so every following step began a fresh list that restarted at one. Eleven blocks are indented into the item they belong to. markdownlint's own autofix would instead have renumbered the steps to 1./1./1., preserving the broken structure. A table row in `docs/guides/research-skills-usage.md` was also missing its leading pipe.
- Two CI guards that could not tell a link from a document showing what a link looks like. `rules/workflow-documentation.md` grants itself that exception in prose — a rule defining a no-go zone has to display the forms it forbids — but the greps enforcing reference direction and relative links did not honour it, so `rules/adr-management.md` had to carry a fabricated path in its counter-example. Both now share one extractor in `scripts/check-markdown-links.py`, which skips code spans and fences, matches a fence at any indent (the previous one required column zero, and blocks nested in list items are indented) and closes a fence on its own marker. `scripts/test-markdown-links.py` asserts it against a fixture tree; two seeded defects confirmed the test can fail.

### Changed

- markdownlint blocks instead of advising. It was added advisory on a stated plan — run the default rule set in full, collect the real violations, then enforce — and the collection is done: 142 findings under the version the action pins, and 1,963 under a newer one, where a single new table rule accounts for 1,821. Ninety-four were cleared by the tool's own autofix, run with ordered-list renumbering held back because its fix turns a list continuing from an earlier one into a fresh list. The rest were fixed by hand: 16 angle-bracket placeholders into code spans, languages onto two fences, three ambiguous adjacent blockquotes joined. Two rules are off rather than obeyed — bold labels used to group a list, which the repository writes that way throughout and five snapshot records can never change, and the top-level heading requirement for a pull request template that opens under GitHub's own heading. The table rule is pinned to `compact`, since left at its default it measures each table against whichever style is closest and leaves findings its own fixer cannot resolve when two tie. Zero findings now, under both versions.
- The root `.markdownlint.json` becomes `.markdownlint.yaml`, so each decision carries its reason inline. `docs/adr` and `docs/designs` carry nested configurations that extend it and switch off the rules whose remedy would restructure prose. `tests/fixtures` leaves the globs: those files are deliberately malformed inputs for the validators, so linting them measures nothing and an autofix pass could rewrite a test's input.
- `rules/adr-management.md` 1.0.0 → 1.1.0: markup maintenance on a written ADR is not a content revision. "Not edited after writing" protects the words of the record, not a piece of markup that no longer does anything, so a relative link whose target never resolved may be downgraded to its own label text. Repointing it at the target's successor stays forbidden — it asserts a continuity the decision never claimed — as does rewriting the prose around it. `docs/LANGUAGE_SCHEME.md`, which quoted the old wording, follows.
- `skills/INDEX.md` is now generated from each skill's own frontmatter by `scripts/sync-skills-index.py`, and carries the `tags` and `triggers` that AGENTS.md §4 ranks on. The registry held neither before, so the first two steps of the documented matching algorithm had no data to rank and it fell through to description similarity without saying so; 18 of 61 descriptions had also drifted from the skills they register. CI now compares the registry with the skills field by field (`--check`) instead of comparing counts, and `scripts/test-skills-index.py` gives that comparison a denominator — `--check` measures the generator's output against a file the same generator wrote, so on its own it cannot see a parsing bug. Contributors regenerate instead of hand-editing; `CONTRIBUTING.md` and the PR template say so, and `triggers` is now listed among the required frontmatter fields it had always been in practice.
- Reference direction between asset layers is stated in `docs/architecture/terminology.md` §II and enforced in CI: a Spec, Protocol or Rule names the capability that applies it and never links into `skills/`. Twelve such links were removed from `rules/roadmap-quality.md`, `rules/standards-agent-testing.md`, `specs/nats-messaging.md`, `specs/cross-team-contract.md` and `specs/agent-test-modeling.md` — a contract that links to one implementation of itself depends on it, and a copy taken into another repository carries a path to a capability that is not there. The deliberate Spec-and-Rule pairing is recorded in the same section, so their mutual references are not re-reported as a dependency cycle.
- `requirement-intake-triage-update-contract.md` moved from `specs/` to `.cortex/contracts/`, mirroring the vendored contracts under `.cortex/vendor/contracts/`, and its `artifact_type` changed from `guide` to `contract`. It is a contract instance — one outward promise to one named consumer — not a data contract describing a kind of artifact, which is what `specs/` holds. `contract_version` stays at 1.0.0 because the promise itself did not change.

## [0.1.0] — 2026-09-09

### Added

- `CODE_OF_CONDUCT.md`、`SECURITY.md`、`.editorconfig`、`.github/`（issue 模板、PR 模板、lint 工作流）: 补齐开源社区标配。SECURITY.md 明确本仓库的安全面只有两处——`bin/cortex` 这个会创建删除符号链接的 shell 脚本，以及技能内容本身（措辞若诱导 Agent 执行破坏性或越权操作即构成安全问题），并区分了哪些属于 / 不属于安全问题。CI 分三个 job：链接检查与注册表一致性阻断合并，markdownlint 暂设 `continue-on-error` —— 仓库已按 MD009/012/022/031/040/047 清理完毕，但默认规则集远不止这些，尚未在 CI 上实跑过完整规则，先以顾问模式收集真实违规再改为阻断。
- `README.md`: 补「能力概览」节。此前访客第一眼看不出这仓库有什么——现按五个领域列出 55 个技能的分布与代表能力，并说明与同类资产库的四点区别（四层资产分离、编排与原子分层、判据外置、vendored-only 分发）。

- `docs/guides/roadmap-planning-usage.md`: new guide — 路线图规划链路的面向使用者入口。此前这条链的九个技能没有任何用户侧入口：`skills/INDEX.md` 是给 Agent 匹配用的一行描述，各技能 README 的「相关技能」是点对点的拼不出全貌，`orchestrate-roadmap-planning` 的 SKILL.md 里虽有完整九步表但那是写给 Agent 的。本指南按「你处在什么状态」组织而非复述执行顺序（顺序的权威定义仍在 SKILL.md，避免 SSOT 双写），重点讲三件容易撞上的事：晋升时的容量分配 halt 为什么不自动填、治理文档全空时为什么不从零盘问、依赖为什么必须排在晋升之前。

- `rules/roadmap-quality.md` (v1.0.0): new rule — 路线图制品的 5 维评审清单。`rules/` 下原有 requirement / functional-design / technical-design / task / test-case / test-coverage 六份判据 rule，roadmap 是治理链上唯一缺判据的一层。本文件是路线图判据的**唯一权威源**：`define-roadmap`（生产侧）、`plan-next`（诊断侧）、`review-roadmap`（评审侧）三方引用同一份表述，避免重蹈 `rules/requirement-quality.md` 与 `review-requirements` 各维护一套判据的覆辙。反模式清单由本 rule 自身的五维结构推出，每条对应一维判据的失效形态；涉及的产品管理通用概念在「判据依据」节回溯至公开出版物（《Product Roadmaps Relaunched》、Intercom RICE、《Lean UX》、《Escaping the Build Trap》等）。**制定过程中调研过三个开源 PM 技能仓库，初版曾把其中之一列为判据出处，随后发现该仓库为 CC BY-NC-SA 4.0——其非商业与相同方式共享条款与本仓库 MIT 不兼容，衍生声明会造成许可证冲突。已重写反模式使其从本 rule 的判据结构自洽推出，并将概念出处回溯到一手公开著作，不再声明衍生自任何外部仓库。**另两个调研对象（Apache-2.0 与 MIT）虽许可证兼容，本 rule 亦未复制其表达，故同样不作衍生登记——按 `specs/skill-source-modeling.md`，`SOURCES.yaml` 只登记 vendored 的 Skill 本地副本，行业通用概念与文档链接不登记。
- `skills/map-item-dependencies/SKILL.md` (v1.0.0): new skill — 识别 backlog 与 roadmap 条目间的五类依赖（技术 / 团队 / 外部 / 知识 / 顺序），写入条目 `depends_on`，产出依赖图、需解决时点与削减建议。此前本仓库对条目间依赖完全空白：`promote-roadmap-items` 只按 priority 与容量排序，一个「P0 但被仍在 backlog 的条目阻塞」的项会被正常拉进 Now 占着容量不产出。发现依赖环时 halt 并输出环路径，不自行选边打破——拆哪条边是范围决策，超出依赖登记的职责。`depends_on` 字段语义借用 `rules/task-quality.md` 的既有约定；已知债务：backlog-item 目前无 spec，结构只由 `capture-work-items` 的输出模板隐式定义，若该制品字段继续增长应补 `specs/backlog-item-modeling.md`，此处仅记账不代表已解决。
- `skills/update-roadmap/SKILL.md` (v1.0.0): new skill — 路线图日常运维入口：改状态、挪期并计算下游影响、产出「本次变更了什么」摘要。此前 `define-roadmap` 管从零建、`promote-roadmap-items` 管跨层晋升降级，两者之间的日常动作无处安放。与 promote 的分界写死为「改变层级的归 promote，不改变层级的归本技能」——把 Now 项推迟两周是本技能，把它挪到 Next 是 promote。改为 `at risk` / `blocked` 时阻塞原因与缓解方案缺一不写入；依赖数据缺失时明确声明「下游影响未计算」而非假装已分析。
- `skills/review-roadmap/SKILL.md` (v1.0.0): new skill — 按 `rules/roadmap-quality.md` 评估既有路线图，输出 findings-list。IO 契约对齐既有的 `review-requirements`（`document-artifact` 进、`findings-list` 出）。输入除 roadmap.md 外还包含佐证源——`strategic-goals.md` 与 Now 层引用的 backlog 条目：判据中有三条的数据在条目 frontmatter 里（`strategic_goal_id` / `depends_on` / `priority_decision`），只读 roadmap.md 的话这些维度结构上就无从求值，负责检查的技能读不到要检查的东西。读不到佐证源时标「无法评估」，不得记为通过。**判据不内嵌**，全部引自 rule，这是与 `review-requirements` 现状的刻意差异。变更频率维度依赖 git log，写明了「发现 → 执行 → 缺失处理」路径：非 git 仓库、浅克隆或无提交历史时标注为「无法评估」并说明原因，不得静默跳过或据此推断通过。判据文件缺失时 halt——没有基准就评估，产出的会是一份看似权威实则无依据的清单。**不输出 mode**：编排层的上下文检测由编排层自己做。
- `skills/orchestrate-roadmap-planning/SKILL.md` (v1.0.0): new skill — 编排技能，按固定 9 步串联从战略目标到晋升的原子技能。存在价值是**提前满足下游 halt 条件**：手工调用最常见的浪费是做到 `promote-roadmap-items` 才发现缺容量分配、halt、回头跑 `define-roadmap` 再重来（该回退在 promote 的示例 2 里被当成正常流程写着）。每步标注必选 / 默认 / 推荐三档，档位是各原子技能既有约束的机械映射而非本层判断——步骤 1、2 必选因为对应 promote 明写的 halt 条件；步骤 4 双档位对应 promote 对「全部 unset」halt、对「部分 unset」仅跳过的两种行为；步骤 8 推荐因为 `archive-milestone` 自身 `apply` 默认即为 dry-run。档位同时决定失败语义，这是相对 `orchestrate-code-review` 单一 halt 规则的有意偏离（那里各原子技能彼此独立，这里步骤间有数据依赖）。含与 `plan-next` / `orchestrate-governance-step` 的正交边界表，禁止反向调用以避免调用环。

### Fixed

- 机翻残留清理：164 处重复直译标题（`## 行为（行为）`、`## 限制（限制）`、`## 示例（示例）`、`## 使用场景（用例）`、`### 输入（输入）` 等）改为「中文 + 英文原词」形态；27 处明显误译的技术词修正——preflight 曾译作「飞行前」、flaky tests 译作「片状测试」、AI agent 译作「人工智能代理」、happy path 译作「幸福之路」、prefer 一律译作「更喜欢」。这类痕迹分布在 29 个文件，是公开仓库最扎眼的一类问题。
- markdown 格式清理：全仓 199 处违规清零——117 个代码块补语言标签（保守推断，判不准的标 `text`，丢高亮但不改语义）、48 处标题与代码块前后补空行、21 处文件结尾换行、连续空行合并。`.cortex/vendor/` 下的外部契约不重排格式。
- 坏链修复：3 条路径笔误（`docs/adr/0009` 的 `../../../` 深度错误、`docs/architecture/README.md` 指向不存在的 `adrs/`、`rules/claude-md-management.md` 的 README 相对路径）。剩余 8 条全部位于历史 ADR 且指向已删除的资产（`specs/skill.md`、`skills/review-code` 等），属准确的时点记录，按 `rules/adr-management.md`「ADR 写入后不改」保留。
- `CHANGELOG.md`: `[Unreleased]` 日期从 2026-05-15 更新为 2026-09-09。

- `docs/guides/proactive-suggestions.md`: 补 5 条路线图相关入口（规划 / 体检 / 运维 / 优先级 / 依赖）。此前今天涉及的 10 个技能里只有 `capture-work-items` 在表内，本该作为唯一入口的 `orchestrate-roadmap-planning` 未列。同时移除 `tidy-repo` 一行——该技能在 `skills/` 与 `skills/INDEX.md` 中均不存在，是条死引用。ADR 0003 / 0004 与 2026-05-08 孤儿技能清理设计文档中的 `tidy-repo` 引用保留不动：前者按 `rules/adr-management.md` 写入后不改，后者是记录该技能曾为孤儿的历史快照。
- `docs/references/README.md`: 注册新指南，避免孤立文档。

- `skills/define-roadmap/SKILL.md` (v3.2.0 → v4.0.0): 补上一个悬空的除数。`promote-roadmap-items` 的容量公式是「百分比 × cycle 总容量」，但「cycle 总容量」全仓仅出现一次且是除数，从未有技能负责生成——容量护栏实际算不出结果。新增第 8 步采集**总容量基线**（人数 × 周期 − 开销，按有效工时 60–70% 折算），写入容量分配章节表头。同时明确缓冲语义：百分比相对于**有效容量**而非日历容量，未规划工作（紧急问题、快速 win、他团队请求）的余量在折算时已让出，不再单列槽位——原规则要求百分比之和 = 100% 且全部指向 strategic_goal，没有给未规划工作留位置。成功指标从「优先量化」的软要求改为**强制三元组**（当前值 / 目标值 / 参考系），与 `plan-next` 自检对齐：此前生产方不保证的格式，消费方在强制检查，正常产出的路线图过不了下游自检。取 MAJOR 而非 MINOR，因为按旧版产出的既有 roadmap.md 不再通过新自检。另补关键举措的可证伪假设句式与里程碑的结果句式（此前只有「成果导向」的要求而无模板，关键举措易退化成名词短语），以及可选章节「本轮明确不做」（此前「不属于路线图的需求默认不做」是隐式规则，读者看不到被排除了什么）。
- `skills/prioritize-backlog/SKILL.md` (v2.0.0 → v2.2.0): RICE 的 Impact 补齐为五档 `3 / 2 / 1 / 0.5 / 0.25`。原刻度只有 1/2/3，下限为 1 意味着「几乎没影响」与「中等影响」拿同样乘数，琐碎条目只能靠 Reach 与 Effort 拉开差距，结果是低价值项分数虚高。Confidence 补三档锚点（`100%` 有数据支撑 / `80%` 有部分证据 / `50%` 拍脑袋）——原文只写「0-100%」无参照，同一批条目重跑两次会飘。新增 `status: declined` 终态：MoSCoW 的 `Won't` 原本映射到 P3，而 P3 默认进 Later，「明确不做」的条目会永远躺在那里参与每一轮全量重评。新增 `strategic_override` 留痕字段：最终 priority 高于框架结论时必填理由，避免「用户逐项确认」沦为不留证据的旁路。
- `skills/promote-roadmap-items/SKILL.md` (v1.0.0 → v1.2.1): 定义 Now 层 WIP 默认上限 3–5（原反模式引用「WIP 限制」但该量全仓仅此一处提及、从未定义）。补 Now 层依赖准入护栏：存在未决前置的条目不得进 Now，最高只能到 Next；`depends_on` 缺失时不静默放行而是提示先跑 `map-item-dependencies`。补 `status: declined` 条目跳过规则。容量公式的分母改为指向 `define-roadmap` 写入的总容量基线。
- `skills/capture-work-items/SKILL.md` (v2.0.0 → v2.0.1): 第 140 行指示 agent 写 `status: capture`，但同文件三个输出模板写的是 `captured`，`promote-roadmap-items` 消费的也是 `captured`——照该指令执行会产出下游读不到的条目。
- `skills/design-strategic-goals/SKILL.md` (v1.2.0 → v1.3.0): 补冷启动降级模式。该技能第 2 步本就有推导机制（逆向拆解北极星：「要让 NSM 增长，什么必须为真？」），但把愿景与北极星列为**必需输入**；冷启动时上游五层（mission → vision → north-star → strategic-pillars → strategic-goals）全空，推导无依据可用，整条链在第一步就断。新增降级路径：两者均缺失时不 halt，改为从仓库实证反推候选目标，提取源按意图密度排序（README / AGENTS.md → CHANGELOG → git log → 代码结构 → backlog → docs 其余）；CHANGELOG 与 git log 分列两级并写明相互独立——冷启动演练中 CHANGELOG 缺失而 git log 恰是最富的来源，捆绑写法会被读成「缺其一就跳过这一级」。安全约束不可省略——实证反推的产物读起来会很自洽很顺畅，恰恰因此最可能是脑补填空：每条候选目标必须附指向具体文件的推断依据，推不出依据的不许写；不得自动持久化；落盘文档头部留「缺上游背书、属临时锚」标记。此改动是 `rules/agentic-artifact-paradigm.md`「提取优先、逼问垫底」在冷启动场景的落地。
- `skills/{define-roadmap,capture-work-items,design-strategic-goals}/SKILL.md` 与三份 README: 清除 12 处指向不存在 ADR 的引用，理由改为内联。其中 7 处写全名（`20260417-unified-value-driven-prioritization-model` / `20260417-work-lifecycle-and-skill-responsibilities`），`docs/adr/` 实际只有 0001–0011；另 5 处简写为「ADR 1 决策 3.4/3.5」，而 `docs/adr/0001-io-contract-protocol.md` 确实存在但内容是 IO 契约协议，读者顺着编号查会查到一份不相干的文档——这比引用一个不存在的编号更容易误导。四条被内联的理由：四框架不聚合（聚合会掩盖框架间的判断冲突）、容量按 strategic_goal 分配（护栏要按目标控制就必须有可归属的分母）、工程健康目标必填且非零（这类工作无战略代言就永远排不进容量）、工作项必填 `strategic_goal_id`（晋升时要按目标归集容量）。
- `skills/{map-item-dependencies,update-roadmap,review-roadmap,orchestrate-roadmap-planning}/README.md`: 为 4 个新技能补 README，对齐 `prioritize-backlog` / `promote-roadmap-items` 的现行格式。补前覆盖率 45/55，补后 49/55。
- `skills/plan-next/SKILL.md` (v13.3.0 → v13.3.1): KPI 三元组格式定义处补指向 `rules/roadmap-quality.md` 的权威源引用，保证与 `define-roadmap` 产出侧措辞同步。

- `skills/prepare-release/SKILL.md` (v1.3.1 → v1.4.0): 补「材料的形态」一节与一条自检项。此前只划受众边界（技术 changelog 装接口/配置/迁移细节，用户 release notes 只写可感知价值），**不管形态**——于是「面向维护者」被读成「可以不可读」，条目长成一条几百字、塞满编号与路径的段落。新增五条形态约束：一条一个意思、结论先行、背景与佐证不进正文、内部记号克制、升级须知单列。**长度阈值刻意不写进本 Skill**：它取决于语种、读者与既往基线，写死一个数换个项目就不成立；Skill 只给判据并指出可从项目最早、最可读的那几版量出基线，阈值与红线归项目自己的 rules。源自 recloud-wright 的实测——该仓库条目中位长度五个月从 119 字涨到 636 字，而最早两版本来是可读的。
- `skills/consume-nats-message/SKILL.md` (v1.1.0 → v1.4.0) + `specs/nats-messaging.md` (v1.0.0 → v1.1.0): optional `consume_pattern` wildcard consumption mode for `.cortex/nats.yaml` — opt-in, backward compatible with existing `consume_subjects` per-subject mode. Single wildcard durable consumer instead of one-per-subject; per-message dynamic contract resolution (exact-subject match against vendored contract frontmatter); messages on draft/unknown subjects are never blindly acked — inline single-message Bootstrap drafts the contract, then the message is term'd + DLQ'd with `X-DLQ-Reason: awaiting contract confirmation` (surfaced in the aggregate receipt's new `awaiting_confirmation` bucket) rather than nak'd forever (which would silently vanish past `max_deliver` with no DLQ trail). Known limitation: JetStream `ack_wait`/`max_deliver` are durable-level, so wildcard mode shares one retry policy across all subjects. The skill now also documents NATS MCP capability discovery, exact-subject vs wildcard routing, local-contract-first behavior, and draft acknowledgment guardrails. v1.4.0 adds two pre-filter rules discovered during real-world dogfooding: self-sourced messages (`X-Source == service_source`, an echo of the consumer's own outbound traffic under a shared bidirectional subject prefix) and DLQ copies re-matching the same wildcard filter (identified by the self-attached `X-DLQ-Original-Subject` header, not a bare `.dlq` subject-suffix match — a suffix match would misclassify a real business subject that happens to end in `.dlq`) are ack'd directly, skipping contract resolution — without this, both cases would incorrectly generate spurious Bootstrap drafts. A missing `X-Source` header is never treated as a self-sourced match; it falls through to the normal decode-and-reject path instead. `publish-nats-message` unchanged — no analogous discovery problem on the producer side
- `skills/manage-secret/SKILL.md` (v1.1.0): new skill — store, look up, or delete a personal credential (SSH, database, website, API key, certificate) in the macOS Keychain under a `<project>-<env>-<kind>` naming scheme via the `security` CLI, with an optional pointer-only entry (never the secret itself) in the current project's Claude Code memory. Guards against silent overwrite (probes for an existing same-named entry before writing) and never echoes secret values in confirmation messages or memory files.
- `skills/refine-skill-design/SKILL.md` (v1.4.0 → v1.5.0): added repository asset-boundary, tool-adaptation, and local contract compliance audit checks so skill refinement can detect Skill / Spec / Protocol / Rule mixing, missing MCP capability mapping, and AGENTS.md policy conflicts.
- `specs/functional-design-modeling.md` + `rules/functional-design-quality.md`: new business/product-facing design spec (artifact_type `functional-design`) — functional modules, business workflow, exceptions, with conditional state-diagram & permission-matrix sections
- `specs/technical-design-modeling.md` + `rules/technical-design-quality.md`: new engineering-facing design spec (artifact_type `technical-design`) — architecture, components, database, API contracts, tech selection
- `specs/requirement-modeling.md`: mandatory 「目标」(Objective) body section — requirement-level outcome statement, distinct from upstream goals (referenced via `parent`)
- `specs/requirement-modeling.md`: conditionally-mandatory 「业务规则」(Business Rules) section with escalation triggers (rule-set-as-deliverable / one rule cited by ≥2 ACs / state machine / compliance SSOT); §5.3 declarative-form rules + rule↔AC boundary
- `specs/requirement-modeling.md` §1: explicit acceptance-criteria-centric content philosophy
- `bin/cortex` POSIX sh script: 5 subcommands (install / update / clean / status / uninstall) for cross-IDE asset management
- Codex-compatible skill sync via per-skill symlinks under `~/.agents/skills/<skill>`
- `docs/adr/0010-installation-strategy.md`: decision record for XDG canonical path + bin/cortex strategy
- `rules/*.md`: added `recommended_scope` field and standardized `# Rule: ...` H1 titles to all 12 rules files

### Changed

- Split the `design` artifact into `functional-design` + `technical-design` along the business/engineering audience boundary. Chain is now sequential + conditional: `requirement → functional-design → technical-design → task` (functional layer skippable for pure-tech work; `technical-design.parent` is polymorphic over functional-design|requirement; `task.parent` always technical-design)
- `specs/task-modeling.md`: `parent` retargeted to technical-design; v2.0.0 → v2.1.0
- `specs/requirement-modeling.md` (→ v4.0.1) / `specs/test-case-modeling.md` (→ v1.0.1) / `specs/spec-modeling.md` (→ v2.0.1): repointed design-modeling references to the new split specs
- `docs/ARTIFACT_NORMS.md`: replaced `design` artifact_type with `functional-design` + `technical-design` (canonical-source / path / timestamp tables)
- `docs/designs/*`: migrated 2 existing design docs to `artifact_type: technical-design` and renamed to the `-technical-design.md` suffix
- `specs/requirement-modeling.md`: v3.0.0 → v4.0.0 (BREAKING — new mandatory 目标 section makes prior requirements non-compliant); id `_V3` → `_V4`
- `rules/requirement-quality.md`: 7 → 8 mandatory fields (added 目标); conditional 业务规则 completeness + rule↔AC traceability checks; v1.0.0 → v2.0.0
- `README.md` §安装与使用: replaced IDE-specific prompt blocks with `git clone` + `cortex install` quickstart
- `AGENTS.md` §2: added canonical install path reference for Agent direct access

### Removed

- `specs/design-modeling.md` + `rules/design-quality.md`: superseded by the functional/technical design split
- Vercel `skills` CLI as recommended install path (now superseded by `cortex install`; existing Vercel CLI installs remain compatible via coexistence mode)
