# Claude Plugin — skill subset

This directory configures the AI Cortex plugin for Claude. `marketplace.json` lists a **subset** of skills from `skills/` (not the full catalog).

- **Canonical list**: See [skills/INDEX.md](../skills/INDEX.md) and [manifest.json](../manifest.json).
- **Maintenance**: When you add or promote high-priority skills, update the `skills` array in `marketplace.json` so the plugin exposes them.
