name: design-solution
status: validated

primary_use: Produce a validated design document from requirements (architecture, components, data flow, error handling, testing strategy, trade-offs) with HARD-GATE before implementation; serves as single source of truth for breakdown-tasks.

inputs:
  - validated requirements document (path or content)
  - optional project context, constraints, existing patterns or ADRs

outputs:
  - validated design document (docs/designs/YYYY-MM-DD-<topic>.md; snapshot)
  - trade-off analysis (2-3 approaches with pros/cons/best-for)

scores:
  agent_native: 5
  cognitive: 5
  composability: 4
  stance: 5

asqm_quality: 19

overlaps_with:
  - nesnilnehc/ai-cortex:analyze-requirements
  - nesnilnehc/ai-cortex:refine-skill-design

market_position: differentiated

tags: [writing, eng-standards, documentation]
