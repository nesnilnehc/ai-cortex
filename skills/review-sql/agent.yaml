name: review-sql
status: validated

primary_use: Review SQL and query code for injection, parameterization, indexing/performance, transactions, NULL/constraints, portability. Language-only atomic skill; output is a findings list.

inputs:
  - code scope (files, snippets, or diff containing SQL)

outputs:
  - findings list (Location, Category=language-sql, Severity, Title, Description, Suggestion)

scores:
  agent_native: 5
  cognitive: 4
  composability: 5
  stance: 5

asqm_quality: 19

overlaps_with:
  - nesnilnehc/ai-cortex:review-codebase
  - nesnilnehc/ai-cortex:review-code
  - nesnilnehc/ai-cortex:review-security
  - nesnilnehc/ai-cortex:review-diff

market_position: commodity

tags: [eng-standards]
