name: bootstrap-project-documentation
status: validated

primary_use: Bootstrap or adapt project docs using project-documentation-template. Initialize (empty) or Adjust (non-empty); use template as target reference in Adjust; repeatable; no empty dirs unless requested; strict kebab-case naming.

inputs:
  - project metadata (name, description, tech stack)
  - scale (small | medium | large, optional)
  - mode override (initialize | adjust, optional)

outputs:
  - Initialize: docs/ directory, VERSION file, summary of created files
  - Adjust: recommendation list (markdown), then applied changes and summary after user confirmation

scores:
  agent_native: 5
  cognitive: 4
  composability: 4
  stance: 5

asqm_quality: 18

overlaps_with:
  - nesnilnehc/ai-cortex:generate-standard-readme
  - nesnilnehc/ai-cortex:write-agents-entry

market_position: differentiated

tags: [documentation, eng-standards, writing]
