name: breakdown-tasks
status: validated
primary_use: Break a design document into an executable task list with dependencies,
  acceptance criteria, and assignee or AI execution hints. v3.0 adopts artifact-contract
  v5.0 unified canonical path docs/tasks/{slug}.md (no date prefix); Stage 0 Norms
  Resolution reads project ARTIFACT_NORMS.md to resolve path_pattern; optional
  upstream_ref input triggers parent: frontmatter emit.
cognitive_mode: interpretive
inputs:
- Design document (path or content)
- Optional scope or priority hints
- Optional upstream_ref (parent design path; triggers parent: frontmatter emit)
- Optional artifact_norms_path override
outputs:
- Task list document at norms-resolved path (default docs/tasks/<slug>.md)
- parent frontmatter field when upstream_ref provided
scores:
  agent_native: 5
  cognitive: 4
  composability: 4
  stance: 5
asqm_quality: 18
overlaps_with: []
market_position: differentiated
validation_gates:
  gate_a:
    name: Agent-ready
    requirement: agent_native >= 4
    status: pass
  gate_b:
    name: Design integrity
    requirement: stance >= 3
    status: pass
